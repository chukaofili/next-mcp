version: '3.8'

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
__DATABASE_DEPENDS_ON__

__DATABASE_SERVICE__

__VOLUMES_SECTION__