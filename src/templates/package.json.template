{
  "name": "<projectName>",
  "version": "0.1.0",
  "private": true,
  "description": "<description>",
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "test": "turbo run test",
    "test:watch": "turbo run test:watch",
    "pipeline": "turbo run build lint test",
    "typecheck": "turbo run typecheck",
    "clean": "turbo run clean && rm -rf node_modules"
  },
  "devDependencies": {
    "turbo": "^2",
    "typescript": "catalog:"
  },
  "engines": {
    "node": ">=24"
  }
}
